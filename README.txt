DigitLab AI refresh files

Files included:
- index.html
- styles.css

Notes:
1. These files do not include or replace any graphics or logo assets.
2. If your current site already uses a logo image, keep that file as-is.
3. In index.html, find the brand section near the top and uncomment/edit the logo <img> line if needed.
4. If your current repo uses a different CSS filename (for example style.css instead of styles.css),
   either rename this file or update the <link rel="stylesheet"> line in index.html.
5. Add an extra alias in ImprovMX:
   info@digitlabai.co.uk -> your mailbox
